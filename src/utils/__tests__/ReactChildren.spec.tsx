import React from 'react';
import {map} from "../ReactChildren";

describe('ReactChildren test', () => {

  it('map', () => {
    map([
      <p>1</p>,
      2
    ], (item, index) => {

    });

  });
});
