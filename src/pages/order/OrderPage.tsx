import React from "react";
import "./OrderPage.scss";

interface IProps {

}

const OrderPage: React.FC<IProps> = (props: IProps) => {
  return (
    <div className={"frame-content"}>
      OrderPage works! 
    </div>
  );
};

export default OrderPage;
